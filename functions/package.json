{
  "name": "tig-music-functions",
  "description": "Tig Music Cloud Functions — the Hermes co-writer proxy (holds secrets; enforces propose-never-act).",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:tig-music"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.0"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  },
  "private": true
}
